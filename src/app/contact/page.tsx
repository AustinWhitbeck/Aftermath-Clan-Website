import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'Contact - Aftermath',
  description: 'Aftermath Clan Contact Info',
}

const ContactPage = () => {
  return (
    <div>Contact Page</div>
  )
}

export default ContactPage